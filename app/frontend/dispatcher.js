/* eslint-disable */
/**
 * This file handles the frontend client's communications with the backend.
 * @module app/frontend/dispatcher
 */

/**
 * Creates the frontend's dispatcher.
 * @param {ViewManager} viewManager - The frontend's view manager.
 */
export function createFrontendDispatcher(viewManager) {

  // SETUP
  // =====

  // TODO: declare variables and generic functions if you need them

  /**
   * Example: declare state
   *
   *     let currentGame;
   *     let currentPlayer;
   */

  /**
   * Example: display a toast message
   *
   *     viewManager.displayToast('Hello World');
   */

  // GAME MANAGEMENT
  // ===============

  // TODO: write game management functions if you need them

  /**
   * Example: start playing a game
   *
   *     viewManager.displayGame(game, player);
   */

  /**
   * Example: display a player's icon (X or O) in one of the board's cells
   *
   *     viewManager.updateBoard(col, row, icon);
   */

  /**
   * Example: add a game to the displayed list of joinable games
   *
   *     viewManager.addJoinableGame(game);
   */

  /**
   * Example: remove a game from the displayed list of joinable games
   *
   *     viewManager.removeJoinableGame(gameId);
   */

  /**
   * Example: leave the current game
   *
   *     viewManager.leaveGame();
   */

  // PLAYER MANAGEMENT
  // =================

  // TODO: write player management functions if you need them

  // COMMUNICATIONS
  // ==============

  // TODO: implement the frontend's communications with WebSockets or the Web Application Messaging Protocol (WAMP).

  /**
   * Example: handle DOM events
   *
   *     viewManager.events.on('createGame', function onCreateGame() {
   *       // The player has clicked the Create Game button.
   *     });
   *
   *     viewManager.events.on('leaveGame', function onLeaveGame() {
   *       // The player has clicked the Leave Game button.
   *     });
   *
   *     viewManager.events.on('joinGame', function onJoinGame(gameId) {
   *       // The player has clicked on one of the games' Join Game button.
   *     });
   *
   *     viewManager.events.on('play', function onPlay(col, row) {
   *       // The player has clicked in a game board's cell.
   *     });
   */

}
